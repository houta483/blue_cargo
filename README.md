This program will scrape the schedules from two urls and print the result in the terminal.

To run the program, please enter the follow commands in your terminal:

1. pip install -r requirements.txt
2. python3 main.py
